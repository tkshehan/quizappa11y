There were no violations in aXe.  I made some changes, and attempted to add a an 
aria-live attribute to an element, only to find it will requre a rewrite of multiple
functions to get the intended effect.

<h> tags are now ordered logically for navigation with H and 1-2-3 keys.